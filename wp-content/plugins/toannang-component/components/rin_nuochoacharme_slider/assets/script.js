wow = new WOW(
        {
            boxClass: 'wow', // default
            animateClass: 'animated', // default
            offset: 0, // default
            mobile: true, // default
            live: true        // default
        }
)
wow.init();
$(document).ready(function() {




});
$(document).ready(function(){
      $('.slicksl').slick({
      	autoplay:true,
      	autoplaySpeed:2000
      });
    });